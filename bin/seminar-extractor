#!/usr/bin/env perl

use strict;
use warnings;

use FindBin qw($Bin);
use lib "$Bin/../lib";

use aliased 'Seminar::Extractor::Source::UH::CS' => 'UHCS';

my $cs = UHCS->new();

use DDP; p
$cs->extract;
